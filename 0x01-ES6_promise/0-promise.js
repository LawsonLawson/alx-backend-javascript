export default function getResponseFromAPI () {
  return new Promise((resolve, reject) => {
    // Simulating an asynchronous operation
    // Resolve the promise without any specific value
    resolve();
  });
}
