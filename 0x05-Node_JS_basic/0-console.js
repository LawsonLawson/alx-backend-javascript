/**
 * Displays a message on the standard output (console).
 *
 * This function takes a string message as input and outputs it to the console
 * using `console.log`.
 *
 * @param {string} msg - The message to display in the console.
 */
const displayMessage = (msg) => {
  console.log(msg);
};

module.exports = displayMessage;
